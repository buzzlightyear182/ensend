<%= form_for([:admin, @commission_type], html: {:class => "form-horizontal"}) do |f| %>
  <% if @commission_type.errors.any? %>
    <div class="alert alert-danger alert-dismissible" role="alert" id="error_explanation">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <h2><%= pluralize(@commission_type.errors.count, "error") %> prohibited this commission_type from being saved:</h2>

      <ul>
      <% @commission_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, :class => "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :label, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :label, :class => "form-control" %>
    </div>
  </div>

   <div class="form-group">
    <%= f.label :icon, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :icon, :class => "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
